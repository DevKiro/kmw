{%- liquid
  if section.settings.background != settings.background and section.settings.background != 'rgba(0,0,0,0)'
    assign blends_with_background = false
  else
    assign blends_with_background = true
  endif
-%}

<style>
  #shopify-section-{{ section.id }} {
    {%- liquid
      if section.settings.background == 'rgba(0,0,0,0)'
        assign section_background = settings.background
      else
        assign section_background = section.settings.background
      endif

      if section.settings.text_color == 'rgba(0,0,0,0)'
        assign heading_color = settings.heading_color
        assign text_color = settings.text_color
      else
        assign heading_color = section.settings.text_color
        assign text_color = section.settings.text_color
      endif
    -%}

    --heading-color: {{ heading_color.red }}, {{ heading_color.green }}, {{ heading_color.blue }};
    --text-color: {{ text_color.red }}, {{ text_color.green }}, {{ text_color.blue }};
    --prev-next-button-background: var(--text-color);
    --prev-next-button-color: var(--section-background);
    --section-background: {{ section_background.red }}, {{ section_background.green }}, {{ section_background.blue }};

    --section-desktop-column: {{ section.settings.blocks_on_desktop | at_most: 6 }};
    --section-mobile-column: {{ section.settings.blocks_on_mobile | at_most: 2 }};
    {%- if section.settings.layout == 'collage' -%}
    --section-collage-column: {{ section.blocks.size | at_most: 2 }};
    {%- endif -%}

    --blocks-row-gap: {{ section.settings.blocks_row_space | default: settings.blocks_column_space }}px;
    --blocks-column-gap: {{ section.settings.blocks_column_space | default: settings.blocks_column_space }}px;

    --section-block-position: {{ section.settings.blocks_position }};
  }

  #shopify-section-{{ section.id }} .list-collections__item-image-wrapper {
    align-items: {{ section.settings.blocks_text_position }};
    display: flex;
    flex-direction: column;
    grid-gap: 4px;
    gap: 4px;
  }

  {%- for block in section.blocks -%}
    #block-{{ section.id }}-{{ block.id }} {
      {% if block.settings.text_color == 'rgba(0,0,0,0)' %}
        {%- assign text_color_rgb = '255, 255, 255' -%}
      {%- else -%}
        {%- capture text_color_rgb -%}{{ block.settings.text_color.red }}, {{ block.settings.text_color.green }}, {{ block.settings.text_color.blue }}{%- endcapture -%}
      {%- endif -%}

      --heading-color: {{ text_color_rgb }};
      --text-color: {{ text_color_rgb }};

      --section-block-overlay: {{ block.settings.overlay_color.red }}, {{ block.settings.overlay_color.green }}, {{ block.settings.overlay_color.blue }};
      {% if block.settings.overlay_gradient != empty %}
        --section-block-overlay: {{ block.settings.overlay_gradient }};
      {% endif %}
      --section-block-overlay-opacity: {{ block.settings.overlay_opacity | divided_by: 100.0 }};

      --section-block-overlay-tinted: {{ block.settings.overlay_tinted }}px;
    }
  {%- endfor -%}
</style>

<div class="theme-section--collection-list section{% unless blends_with_background %} section--flush{% endunless %}">
  <div class="section__color-wrapper">
    <div class="{% unless blends_with_background %}vertical-breather{% endunless %}">
      {%- if section.settings.subheading != blank or section.settings.title != blank -%}
        <header class="section__header container text-container section__header--{{ section.settings.text_alignment }}">
          {%- if section.settings.subheading != blank -%}
            <{{ section.settings.subheading_tag }} class="text-container__subheading heading heading--small">{{ section.settings.subheading }}</{{ section.settings.subheading_tag }}>
          {%- endif -%}
          {%- if section.settings.title != blank -%}
            <{{ section.settings.heading_tag }} class="text-container__heading heading h2">{{ section.settings.title }}</{{ section.settings.heading_tag }}>
          {%- endif -%}
          {%- if section.settings.text != blank -%}
            <div class="text-container__text rte">{{ section.settings.text }}</div>
          {%- endif -%}
          {%- if section.settings.button_text != blank or section.settings.button_link != blank -%}
            <div
              class="text-container__button button-wrapper{% if section.settings.button_appearance contains 'link' %} icon-text icon-text--inline icon-text--reverse icon-text--underlined{% endif %}"
            >
              <a href="{{ section.settings.button_link }}" class="{% if section.settings.button_appearance contains 'button' %} button button--small{% endif %} {{ section.settings.button_appearance }}{% if section.settings.button_appearance contains 'link' %} text--underlined{% endif %}">
                {{- section.settings.button_text }}
              </a>
              {%- render 'icon' with 'nav-arrow-right-small', inline: true, direction_aware: true -%}
            </div>
          {%- endif -%}
        </header>
      {%- endif -%}

      {%- capture section_content -%}
        <div class="list-collections__item-list">
          {%- for block in section.blocks -%}
            {%- capture collection_content -%}
              {%- if block.settings.subheading != blank -%}
                <{{ section.settings.blocks_subheading_tag }} class="heading heading--xsmall" {% if section.settings.reveal_on_scroll %}reveal{% endif %}>{{ block.settings.subheading | escape }}</{{ section.settings.blocks_subheading_tag }}>
              {%- endif -%}
              {%- if block.settings.title != blank -%}
                <{{ section.settings.blocks_heading_tag }} class="heading h6" {% if section.settings.reveal_on_scroll %}reveal{% endif %}>{{ block.settings.title | escape }}</{{ section.settings.blocks_heading_tag }}>
              {%- endif -%}
              {%- if block.settings.link_text != blank -%}
                <span class="heading heading--xxsmall link" {% if section.settings.reveal_on_scroll %}reveal{% endif %}>{{ block.settings.link_text | escape }}</span>
              {%- endif -%}
            {%- endcapture -%}

            {%- if section.settings.layout == 'collage' -%}
              {%- comment -%}
              The logic is as follows:
                - If we have a modulo 3 (3, 6, 9...) we highlight every modulo 3, and every 6 is shifted
                - If we have a modulo 5 (5, 10, 15...) we highlight every modulo 5, and every 10 is shifted
                - If we have a modulo 7 (7, 14, 21...) we highlight every modulo 7, and every 14 is shifted
              {%- endcomment -%}

              {%- liquid
                assign is_highlighted = false
                assign is_shifted = true
                assign modulo_3 = section.blocks.size | modulo: 3
                assign modulo_5 = section.blocks.size | modulo: 5
                assign modulo_7 = section.blocks.size | modulo: 7

                if modulo_3 == 0
                  assign index_modulo_3 = forloop.index | modulo: 3
                  assign index_modulo_6 = forloop.index | modulo: 6

                  if index_modulo_3 == 1
                    assign is_highlighted = true
                  endif

                  if index_modulo_6 == 1
                    assign is_shifted = false
                  endif
                elsif modulo_5 == 0
                  assign index_modulo_5 = forloop.index | modulo: 5
                  assign index_modulo_10 = forloop.index | modulo: 10

                  if index_modulo_5 == 1
                    assign is_highlighted = true
                  endif

                  if index_modulo_10 == 1
                    assign is_shifted = false
                  endif
                elsif modulo_7 == 0
                  assign index_modulo_7 = forloop.index | modulo: 7
                  assign index_modulo_14 = forloop.index | modulo: 14

                  if index_modulo_7 == 1
                    assign is_highlighted = true
                  endif

                  if index_modulo_14 == 1
                    assign is_shifted = false
                  endif
                endif
              -%}
            {%- endif -%}

            {%- assign collection = block.settings.collection -%}

            <a id="block-{{ section.id }}-{{ block.id }}" href="{{ block.settings.link_url | default: collection.url }}" class="list-collections__item {% if is_highlighted %}list-collections__item--highlight {% if is_shifted %}list-collections__item--shift{% endif %}{% endif %} {% if collection_content != blank %}has-overlay{% endif %} image-zoom" {{ block.shopify_attributes }}>
              {%- assign collection_image = block.settings.image | default: collection.featured_image -%}
              <div class="{% if block.settings.overlay_gradient != empty %}gradient {% endif %}list-collections__item-image-wrapper product-item__aspect-ratio aspect-ratio{% if section.settings.image_height != 'natural' or collection_image == blank %} aspect-ratio--{{ section.settings.image_height }}{% endif %} aspect-ratio--{{ section.settings.image_fit }}" style="padding-bottom: {{ 100.0 | divided_by: collection_image.aspect_ratio }}%; --aspect-ratio: {{ collection_image.aspect_ratio }}">
                {%- if collection_image != blank -%}
                  {%- case section.settings.layout -%}
                    {%- when 'grid' -%}
                      {%- capture image_sizes -%}(max-width: 740px) calc(100vw - 48px), (max-width: 999px) calc(50vw - 60px), 480px{%- endcapture -%}

                    {%- when 'carousel' -%}
                      {%- capture image_sizes -%}(max-width: 740px) 80vw, (max-width: 999px) 60vw, 425px{%- endcapture -%}

                    {%- when 'collage' -%}
                      {%- capture image_sizes -%}(max-width: 740px) calc(100vw - 24px * 2), 660px{%- endcapture -%}
                  {%- endcase -%}

                  {%- if section.settings.layout == 'grid' and collection_image.aspect_ratio > 2.5 -%}
                    {%- liquid
                      assign height_constraint = 800 | at_most: collection_image.height
                    -%}
                    {%- if section.settings.reveal_on_scroll -%}
                      {{- collection_image | image_url: width: collection_image.width, height: height_constraint, crop: 'center' | image_tag: loading: 'lazy', sizes: image_sizes, widths: '200,300,400,500,600,700,800,900,1000,1100,1200,1300,1400', reveal: true, class: 'list-collections__item-image' -}}
                    {%- else -%}
                      {{- collection_image | image_url: width: collection_image.width, height: height_constraint, crop: 'center' | image_tag: loading: 'lazy', sizes: image_sizes, widths: '200,300,400,500,600,700,800,900,1000,1100,1200,1300,1400', class: 'list-collections__item-image' -}}
                    {%- endif -%}
                  {%- else -%}
                    {%- if section.settings.reveal_on_scroll -%}
                      {{- collection_image | image_url: width: collection_image.width | image_tag: loading: 'lazy', sizes: image_sizes, widths: '200,300,400,500,600,700,800,900,1000,1100,1200,1300,1400', reveal: true, class: 'list-collections__item-image' -}}
                    {%- else -%}
                      {{- collection_image | image_url: width: collection_image.width | image_tag: loading: 'lazy', sizes: image_sizes, widths: '200,300,400,500,600,700,800,900,1000,1100,1200,1300,1400', class: 'list-collections__item-image' -}}
                    {%- endif -%}
                  {%- endif -%}
                {%- else -%}
                  {%- capture collection_image -%}collection-apparel-{% cycle '1', '2', '3', '4', '5', '6' %}{%- endcapture -%}
                  {%- if section.settings.reveal_on_scroll -%}
                    {{- collection_image | placeholder_svg_tag: 'list-collections__item-image placeholder-background' | replace: '<svg', '<svg reveal' -}}
                  {%- else -%}
                    {{- collection_image | placeholder_svg_tag: 'list-collections__item-image placeholder-background' -}}
                  {%- endif -%}
                {%- endif -%}
              </div>

              {%- if collection_content != blank -%}
                <div class="list-collections__item-info text-container section__header--{{ section.settings.blocks_text_alignment }} section__header--{{ section.settings.blocks_text_position }}">
                  {{- collection_content -}}
                </div>
              {%- endif -%}
            </a>
          {%- endfor -%}
        </div>
      {%- endcapture -%}

      {%- if section.settings.layout == 'grid' or section.settings.layout == 'collage' -%}
        <collection-list
          {% if section.settings.reveal_on_scroll %}
            reveal-on-scroll
          {% endif %}
          class="list-collections list-collections--{{ section.settings.layout }}"
        >
          <div class="container list-collections__wrapper{% if section.blocks.size == 1 %} list-collections--has-single-block{% endif %} list-collections--has-{{ section.settings.blocks_on_mobile }}-mobile-block">
            {{- section_content -}}
          </div>
        </collection-list>
      {%- elsif section.settings.layout == 'carousel' -%}
        <collection-list
          scrollable
          {% if section.settings.reveal_on_scroll %}
            reveal-on-scroll
          {% endif %}
          class="list-collections list-collections--carousel"
        >
          <scrollable-content class="list-collections__scroller hide-scrollbar list-collections__wrapper{% if section.blocks.size == 1 %} list-collections--has-single-block{% endif %} list-collections--has-{{ section.settings.blocks_on_mobile }}-mobile-block">
            {{- section_content -}}
          </scrollable-content>

          <prev-next-buttons class="list-collections__prev-next prev-next-buttons hidden-pocket">
            <button class="list-collections__arrow prev-next-button prev-next-button--prev">
              <span class="visually-hidden">{{ 'general.accessibility.previous' | t }}</span>
              {%- render 'icon' with 'nav-arrow-left', block: true, direction_aware: true -%}
            </button>

            <button class="list-collections__arrow prev-next-button prev-next-button--next">
              <span class="visually-hidden">{{ 'general.accessibility.next' | t }}</span>
              {%- render 'icon' with 'nav-arrow-right', block: true, direction_aware: true -%}
            </button>
          </prev-next-buttons>
        </collection-list>
      {%- endif -%}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Collection list",
  "tag": "section",
  "class": "shopify-section--collection-list",
  "disabled_on": {
    "templates": ["password"],
    "groups": ["header", "custom.overlay"]
  },
  "blocks": [
    {
      "name": "Collection",
      "type": "collection",
      "settings": [
        {
          "type": "collection",
          "id": "collection",
          "label": "Collection"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image",
          "info": "Grid and carousel modes: 1100 x 1400px .jpg recommended / Collage mode: 1320 x 1480px .jpg recommended (highlighted image) and 1320 x 700px .jpg recommended (normal image)"
        },
        {
          "type": "text",
          "id": "subheading",
          "label": "Subheading"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Heading"
        },
        {
          "type": "text",
          "id": "link_text",
          "label": "Link text"
        },
        {
          "type": "url",
          "id": "link_url",
          "label": "Link URL",
          "info": "If none is set, collection URL is used."
        },
        {
          "type": "header",
          "content": "Colors"
        },
        {
          "type": "color",
          "id": "text_color",
          "label": "Text",
          "default": "rgba(0,0,0,0)"
        },
        {
          "type": "color",
          "id": "overlay_color",
          "label": "Overlay",
          "default": "#000000"
        },
        {
          "type": "color_background",
          "id": "overlay_gradient",
          "label": "Overlay gradient",
          "info": "Insert info for this soon..."
        },
        {
          "type": "range",
          "id": "overlay_opacity",
          "label": "Overlay opacity",
          "min": 0,
          "max": 100,
          "step": 1,
          "unit": "%",
          "default": 30
        }
      ]
    }
  ],
  "settings": [
    {
      "type": "checkbox",
      "id": "reveal_on_scroll",
      "label": "Reveal on scroll",
      "info": "Show animation when section becomes visible.",
      "default": false
    },
    {
      "type": "header",
      "content": "Appearance"
    },
    {
      "type": "select",
      "id": "layout",
      "label": "Layout",
      "info": "Collage mode only affects desktop, and will adapt based on number of items.",
      "options": [
        {
          "value": "grid",
          "label": "Grid"
        },
        {
          "value": "collage",
          "label": "Collage"
        },
        {
          "value": "carousel",
          "label": "Carousel"
        }
      ],
      "default": "grid"
    },
    {
      "type": "select",
      "id": "image_height",
      "label": "All image heights",
      "info": "Use to set all collection block images to the same height. Select \"Natural\" to enable orginal image heights for each block.",
      "options": [
        {
          "value": "natural",
          "label": "Natural"
        },
        {
          "value": "short",
          "label": "Short (4:3)"
        },
        {
          "value": "square",
          "label": "Square (1:1)"
        },
        {
          "value": "tall",
          "label": "Tall (2:3)"
        }
      ],
      "default": "natural"
    },
    {
      "type": "select",
      "id": "image_fit",
      "label": "All image background fit",
      "info": "Use to fit all collection block images to cover or contain.",
      "options": [
        {
          "value": "cover",
          "label": "Cover"
        },
        {
          "value": "contain",
          "label": "Contain"
        }
      ],
      "default": "cover"
    },
    {
      "type": "header",
      "content": "Header"
    },
    {
      "type": "inline_richtext",
      "id": "subheading",
      "label": "Subheading"
    },
    {
      "type": "select",
      "id": "subheading_tag",
      "label": "Subheading tag",
      "info": "Used for advanced SEO. \"span\" is default.",
      "options": [
        {
          "value": "h1",
          "label": "h1"
        },
        {
          "value": "h2",
          "label": "h2"
        },
        {
          "value": "h3",
          "label": "h3"
        },
        {
          "value": "h4",
          "label": "h4"
        },
        {
          "value": "span",
          "label": "span"
        }
      ],
      "default": "span"
    },
    {
      "type": "inline_richtext",
      "id": "title",
      "label": "Heading",
      "default": "Collection list"
    },
    {
      "type": "select",
      "id": "heading_tag",
      "label": "Heading tag",
      "info": "Used for advanced SEO. \"h2\" is default.",
      "options": [
        {
          "value": "h1",
          "label": "h1"
        },
        {
          "value": "h2",
          "label": "h2"
        },
        {
          "value": "h3",
          "label": "h3"
        },
        {
          "value": "h4",
          "label": "h4"
        },
        {
          "value": "span",
          "label": "span"
        }
      ],
      "default": "h2"
    },
    {
      "type": "richtext",
      "id": "text",
      "label": "Description",
      "default": "<p>Use description to give your customers insight into your brand.</p>"
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button text",
      "default": "See all"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button link"
    },
    {
      "type": "select",
      "id": "button_appearance",
      "label": "Button appearance",
      "options": [
        {
          "value": "button--primary",
          "label": "Solid"
        },
        {
          "value": "button--outline",
          "label": "Outline"
        },
        {
          "value": "link--animated",
          "label": "Link"
        }
      ],
      "default": "link--animated"
    },
    {
      "type": "select",
      "id": "text_alignment",
      "label": "Text alignment",
      "options": [
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "right",
          "label": "Right"
        }
      ],
      "default": "left"
    },
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "range",
      "id": "blocks_on_desktop",
      "min": 1,
      "max": 6,
      "step": 1,
      "label": "Blocks per row on desktop",
      "info": "Disabled in collage mode. May adapt based on device, number of items, and selected layout mode.",
      "default": 3
    },
    {
      "type": "select",
      "id": "blocks_on_mobile",
      "label": "Blocks per row on mobile",
      "options": [
        {
          "value": "1",
          "label": "One (1)"
        },
        {
          "value": "2",
          "label": "Two (2)"
        }
      ],
      "default": "2"
    },
    {
      "type": "select",
      "id": "blocks_position",
      "label": "Position first block",
      "info": "Desktop only. Use to position first block when blocks do not use all available space.",
      "options": [
        {
          "value": "start",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "end",
          "label": "Right"
        }
      ],
      "default": "center"
    },
    {
      "type": "range",
      "id": "blocks_row_space",
      "min": 0,
      "max": 40,
      "step": 2,
      "unit": "px",
      "label": "Vertical space between blocks",
      "default": 8
    },
    {
      "type": "range",
      "id": "blocks_column_space",
      "min": 0,
      "max": 40,
      "step": 2,
      "unit": "px",
      "label": "Horizontal space between blocks",
      "default": 8
    },
    {
      "type": "header",
      "content": "Blocks"
    },
    {
      "type": "select",
      "id": "blocks_text_position",
      "label": "Text position",
      "options": [
        {
          "value": "start",
          "label": "Top"
        },
        {
          "value": "center",
          "label": "Middle"
        },
        {
          "value": "end",
          "label": "Bottom"
        }
      ],
      "default": "end"
    },
    {
      "type": "select",
      "id": "blocks_text_alignment",
      "label": "Text alignment",
      "options": [
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "right",
          "label": "Right"
        }
      ],
      "default": "left"
    },
    {
      "type": "select",
      "id": "blocks_subheading_tag",
      "label": "Subheading tag",
      "info": "Used for advanced SEO. \"span\" is default.",
      "options": [
        {
          "value": "h1",
          "label": "h1"
        },
        {
          "value": "h2",
          "label": "h2"
        },
        {
          "value": "h3",
          "label": "h3"
        },
        {
          "value": "h4",
          "label": "h4"
        },
        {
          "value": "span",
          "label": "span"
        }
      ],
      "default": "span"
    },
    {
      "type": "select",
      "id": "blocks_heading_tag",
      "label": "Heading tag",
      "info": "Used for advanced SEO. \"h3\" is default.",
      "options": [
        {
          "value": "h1",
          "label": "h1"
        },
        {
          "value": "h2",
          "label": "h2"
        },
        {
          "value": "h3",
          "label": "h3"
        },
        {
          "value": "h4",
          "label": "h4"
        },
        {
          "value": "span",
          "label": "span"
        }
      ],
      "default": "h3"
    },
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "background",
      "label": "Background",
      "default": "rgba(0,0,0,0)"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text color",
      "default": "rgba(0,0,0,0)"
    },
    {
      "type": "header",
      "content": "Spacing"
    }
  ],
  "presets": [
    {
      "name": "Collection list",
      "blocks": [
        {
          "type": "collection"
        },
        {
          "type": "collection"
        },
        {
          "type": "collection"
        },
        {
          "type": "collection"
        },
        {
          "type": "collection"
        },
        {
          "type": "collection"
        }
      ]
    }
  ]
}
{% endschema %}
